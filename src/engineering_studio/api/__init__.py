"""WHAT: Package marker for the (optional) HTTP/WebSocket API layer.
WHY: Reserves an import path (`engineering_studio.api`) for route
definitions if the demo needs a web-facing endpoint; keeps transport
code separate from `agents.orchestrator` per AGENTS.md SS1.
HOW: Empty placeholder. See README.md in this folder for scope and
current status (not yet populated as of the initial scaffolding pass).
"""
