"""WHAT: Package marker for shared pydantic data models/schemas.
WHY: Centralizes typed contracts (Task Specifications, artifact
schemas) for use across agents, the CLI, and the API layer, rather than
passing untyped dicts between modules.
HOW: Empty placeholder. See README.md in this folder for scope and
current status (not yet populated as of the initial scaffolding pass).
"""
